import React from 'react';
import Home from './Home'; // adjust the path if Home.tsx is in another folder

const App: React.FC = () => {
  return (
    <div>
      <Home />
    </div>
  );
};

export default App;
